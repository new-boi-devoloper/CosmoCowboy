using UnityEngine;

namespace EnemyScripts.EnemyManagement
{
    public class WolfEnemyAnimator : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
